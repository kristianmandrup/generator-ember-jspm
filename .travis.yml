language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ember-jspm' ]; then cd .. && eval "mv $currentfolder generator-ember-jspm" && cd generator-ember-jspm; fi

